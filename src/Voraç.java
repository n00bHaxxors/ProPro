/**
 * Created by Markus on 10/05/2017.
 */
/*
public class Voraç {
    public Circuit veri_voras(){
            Circuit resultat=null;
            Candidat iCan(sol.Get_nvertex());
            sol.Inicialitzar_vorac();

            while(!sol.completa() i Candidat_valid()){
                sol.Buscar_Prometedor(iCan);
                if(candidat_valid())
                    sol.Afegir_Candidat(iCan);
            }
            if(sol.completa())
                resultat=new Circuit();

            return resultat;
        }

    private ??? Buscar_Prometedor() {
        Candidat millor (nvertex, invalid);
        iCan = Candidat(nvertex);
        int v = solucio.back();
        float millor_prob = 1;
        while (!iCan.esFi()) {
            if (vorac.ExisteixAresta(v, iCan.actual())) {
                aresta t = vorac.Dades_aresta(v, iCan.actual());
                if (t.prob_atemptat < millor_prob) {
                    millor_prob = t.prob_atemptat;
                    millor = iCan;
                } else if (t.prob_atemptat == millor_prob and t.distancia<vorac.Dades_aresta (v, millor.actual()).
                distancia)
                millor = iCan;
            }
            iCan.seguent();
        }
        iCan = millor;
    }
}
*/